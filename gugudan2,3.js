//2,3단 구현하기

const getGugudanArray = (num, num2) => {
    for (i = 1; i < 10; i++) {
        console.log(`${num} x ${i} = ${num * i}`)
        console.log(`${num2} x ${i} = ${num2 * i}`)
    }
}

let twoAndThreeDan = getGugudanArray(2, 3)
